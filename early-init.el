;;; early-init.el --- early init.el                  -*- lexical-binding: t; -*-
;;; LAST UPDATE : 2023/08/15 18:20:42
;;; Commentary:

;;; Code:

(setq byte-compile-warnings '(cl-functions))
(eval-when-compile (setq byte-compile-warnings '(cl-functions)))
;;; early-init.el ends here
